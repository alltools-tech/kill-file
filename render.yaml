services:
  - type: web
    name: fastapi-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn backend.app:app -k uvicorn.workers.UvicornWorker"
    plan: free